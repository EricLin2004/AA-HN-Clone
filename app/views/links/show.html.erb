<h1>Comments for <%= @link.url_name %> </h1>

<ul>
  <% @comments.each do |comment| %>
    <li>
      <%= comment.body %>
      <small><%=link_to "Reply", new_comment_comment_url(comment) %></small>
    </li>
  <% end %>
</ul>

<%= link_to "Add a comment", new_link_comment_url(@link) %>